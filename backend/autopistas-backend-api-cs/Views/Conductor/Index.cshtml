@model IEnumerable<Project.Models.Conductor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StrNombreConductor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StrApellidoConductor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StrTelefono)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StrNombreConductor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StrApellidoConductor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StrTelefono)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IntCedulaConductor">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IntCedulaConductor">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IntCedulaConductor">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
