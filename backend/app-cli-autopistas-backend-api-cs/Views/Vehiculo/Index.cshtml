@model IEnumerable<Project.Models.Vehiculo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StrObservaciones)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IntIdCategoria)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StrObservaciones)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IntIdCategoria)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StrPlacaVehiculo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StrPlacaVehiculo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StrPlacaVehiculo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
