@model IEnumerable<Project.Models.Mercancia>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StrNombreMercancia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StrDescripcionMercancia)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StrNombreMercancia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StrDescripcionMercancia)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IntIdMercancia">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IntIdMercancia">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IntIdMercancia">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
